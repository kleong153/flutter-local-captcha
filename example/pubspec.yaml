name: example
description: "An example app for LocalCaptcha package."
publish_to: 'none'

version: 1.0.5

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  local_captcha:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
